# Copy source of truth

Website copy currently lives in `src/data/siteCopy.ts` and member information lives in `src/data/members.ts`.

The supplied reference images did not include a separate, verified Traditional Chinese copy document. Suspect Chinese text from the preliminary static build has therefore been replaced with `[TC copy pending]`. Replace those markers only with client-approved Traditional Chinese text; do not auto-translate or convert it to Simplified Chinese.
